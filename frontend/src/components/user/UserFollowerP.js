import React from "react";
import styled, { css } from "styled-components";

function UserFollowerP({ userNickname, userPhoto, userSeq }) {
  console.log(userNickname);
  return (
    <div>
      <img src=""></img>
      <span>{userNickname}</span>
    </div>
  );
}

export default UserFollowerP;
